# Tabulate article descriptions

Produce a table describing articles in specific directories.

## Usage

``` r
tabulate_articles(dirs = c("Accepted", "Submissions"))
```

## Arguments

- dirs:

  The directories containing articles to be searched and tabulated. If
  TRUE, then all directories will be searched.
