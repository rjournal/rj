# List articles.

List all articles in common directories.

## Usage

``` r
active_articles(include = c("Submissions", "Accepted"))

accepted_articles()
```

## Arguments

- include:

  A character vector of directories to include.

## Details

- `active_articles`: `Submissions/`, `Accepted/`

- `accepted_articles`: `Accepted/`
