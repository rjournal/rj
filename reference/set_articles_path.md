# Set the directory of the articles repository

This path is used to locate articles on your file system. If this path
is not specified, the path will default to the root of the repository
which contains the working directory

## Usage

``` r
set_articles_path(path)
```

## Arguments

- path:

  Articles path
