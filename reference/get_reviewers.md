# Find all reviewers for submissions being handled by a given editor

Find all reviewers for submissions being handled by a given editor

## Usage

``` r
get_reviewers(editor)
```

## Arguments

- editor:

  The editor or associate editor handling the submissions

## Value

A data frame of reviewers
